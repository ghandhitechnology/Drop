[build]
builder = "RAILPACK"
buildCommand = "npm ci"

[deploy]
startCommand = "HOST=0.0.0.0 npm run start --workspace=@drop/backend"
healthcheckPath = "/v1/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
