import { HistoryScreen } from '../../src/features/history/HistoryScreen';

/** The running record. Reached from the camera, and from a confirmation. */
export default function History() {
  return <HistoryScreen />;
}
