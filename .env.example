OPENROUTER_API_KEY=
DATABASE_URL=
USAGE_ENFORCEMENT=on
USAGE_LEGACY_POLICY=allow

# RECOGNIZE_PIPELINE=split # mono|split rollback switch for /v1/recognize, default split
# HOST= # backend bind host override
# PORT= # backend bind port override

# Deploy credentials — read from root .env by railway/eas CLIs, not by app code
# RAILWAY_TOKEN=
# RAILWAY_API_TOKEN=
# EXPO_TOKEN=
